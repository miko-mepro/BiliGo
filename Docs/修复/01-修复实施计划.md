# Main/ 代码审计修复实施计划

> 制定日期：2026-07-28
> 依据：`TODO-任务列表.md`（45 条）+ `00-汇总报告.md` + 独立探索验证
> 验证结论：45 条中除 stream.test.ts 文件已存在（仍缺 handleTestConnection 用例）、titleRequestedRef 已部分加固外，**其余问题全部确认仍然存在**
> 修复范围：约 37 条 —— 🔴8 全修、🟠12 修（TODO-21 降级为验证）、🟡14 修（TODO-32 降级）、🔵3 顺手修（38/40/41），其余 8 条明确跳过
> 执行方式：分 10 批，每批结束跑 `npm test && npm run typecheck` 通过后 git commit 一次（中文提交信息，含 TODO 编号）

---

## 探索验证补充事实（修复时可直接信任）

以下为制定本计划时对源码的实际核查结论，修复实施者无需重复验证：

1. **tsconfig**：app/node 两份均无 strict，各有 `/* Linting */` 注释区块可放置开关；`test/` 目录已被 exclude，不受 strict 影响。
2. **sanitize.ts**：SENSITIVE_KEYS 当前为 `['cookie','authorization','sessdata','set-cookie','x-install-id','install-id','installid','x-openrouter-api-key']`；对 key 做 `toLowerCase()` 后**精确匹配**；递归无循环引用保护；**运行时代码零调用**（仅被 `shared-types/index.ts:144` re-export）；无任何测试。
3. **SettingsPanel.tsx handleSave（176-243 行）**：for...of 中逐个 `await chrome.permissions.request({origins:[pattern]})`——第一个 await 后即脱离用户手势同步栈；`chrome.permissions.request` 的 origins 参数**本身支持数组**；`origin-pattern.ts` 已有 `resolveOriginPattern` / `isBuiltInProviderOrigin` 可复用；`SettingsPanel.test.tsx` 已 mock permissions.request。
4. **useResizable.ts:47-48**：模块顶层 `window.innerWidth/innerHeight`，使用点在 81-82 行参数默认值处。
5. **Panel-settings.test.tsx**：8 用例全部是"手动调 mock 再断言 mock 自身"的伪测试；import 了 `Panel` 和 `renderPanelSnapshot` 但从未使用；同目录 `SettingsPanel.test.tsx` 有成熟的 testing-library render + `createMockPort` 模式可照搬；全局 chrome mock 在 `test/setup.ts`。
6. **content/index.tsx**：MutationObserver 无 pagehide/disconnect 终点；模块顶层已有 `observer`/`mountedRoot`/`mountedHost` 变量可在闭包内清理。
7. **ErrorDisplay.tsx**：已 `useChat()`（当前仅取 dispatch）；`useChat()` 返回值含 `sendMessage` 与 `state.messages`；注意 chat-context 的 `send()` 会**追加新 user 消息**（重发会产生重复气泡，产品上等价"用户手动重发"，可接受）。
8. **stream.ts**：三处 void 调用在 567-586 行；各 handler 的 **try 块之前**有 `readBiliAgentSettings`/`createModel`/`WorkingMemoryStore.create`/`streamText` 等可抛异常调用（rejection 被 void 静默吞掉，既不 postError 也不发 done）；既有错误上报模式为 `postToPort` + `inferErrorCode` + `friendlyMessage`；标题生成路径已有 `AbortSignal.timeout(10_000)`（474 行，可作仓库超时惯例）。
9. **slang-understand.ts 189-201**：`update({triedKeywords:[normalized]})` 整字段覆盖；`query-expand.ts:221-234` 的 `recordToMemory` 有现成"读取-合并-去重"模式可照抄；`UnderstandMemoryStore` 接口已含 `get()`。
10. **bilibili-client**：全目录 grep `signal|AbortSignal|timeout` **零匹配**。fetch 位置：`hot.ts:30` / `tags.ts:37` / `suggest.ts:30` / `wbi.ts:109（fetchSigned）,136（fetchWbiKeys）`；`search.ts` 无直接 fetch，经 `wbiFetch(url, init)` 透传 RequestInit；wbiFetch 有 401/-352 重试（同一 signal 会跨两次请求）；wbi.ts 有 `__setWbiKeyCacheForTests` 测试钩子。
11. **session-memory.ts**：`updateSessionMemory`（49-84 行）与 `getSessionMemory`（21-41 行）均无 try-catch，`chrome.storage.session.set` 抛异常时 reject 而非返回 false，违反 `Promise<boolean>` 契约；当前 src 无调用方（仅测试使用）。
12. **TestConnectionButton.tsx:153**：`port.postMessage` 裸调用；Port 断开时同步抛异常导致 status 永久卡 `testing`、按钮永久 disabled；失败路径应复用 `setStatus("fail") + setErrorMessage + cleanup()`。
13. **provider-factory.ts**：`createModel` switch 三 case（openai/anthropic/gemini）无 default，运行时脏 format 隐式返回 undefined，且爆点在 stream.ts try 之外（与第 8 条联动被 void 吞掉）。
14. **working-memory.ts**：`update`（43-57 行）read-then-write 非原子；四个静态方法 `create/get/update/release`；工具侧经闭包绑定 traceId 的窄接口 `{get, update}` 消费，修复需保持签名兼容。
15. **配置现状**：manifest.json 缺 action/CSP/web_accessible_resources；package.json 缺 engines/sideEffects 且 version 0.0.0 与 manifest 0.1.0 不一致；vitest.config.ts 缺 coverage；eslint.config.js globals 仅 browser。
16. **缺失测试**：`test/tools/ask-clarification.test.ts`、`test/tools/bilibili-search.test.ts`、`test/bilibili-client/search.test.ts`、`test/bilibili-client/wbi.test.ts`、`test/lib/shared-types/sanitize.test.ts`（`test/lib` 目录不存在）；`test/background/stream.test.ts` 已存在但缺 handleTestConnection 用例。
17. **依赖版本**：vite ^8.1.1、vitest ^2.1.9、@crxjs/vite-plugin ^2.7.1、@playwright/test ^1.62.0、typescript ~6.0.2、react ^19.2.7。
18. **scripts**：`test: vitest run`、`typecheck: tsc -b`、`lint: eslint .`、`build: tsc -b && vite build`、`test:e2e: playwright test`。

---

## Batch 0：基线确认与 strict 评估（不改代码，无 commit）

1. `npm test && npm run typecheck && npm run lint` 确认基线全绿（不绿先停，记录既有失败）
2. `npx tsc -p tsconfig.app.json --strict --noEmit` 统计 strict 错误量；`tsconfig.node.json` 同理（node 侧只含 vite.config.ts，预期 0~个位数）
3. **TODO-21（降级为验证）**：`npm ls vite vitest @crxjs/vite-plugin` 确认依赖树无重复 vite。既有事实是 `npm test`/`npm run build` 当前可跑，说明兼容性事实成立；只记录结论，不动版本。升级 vitest 3 是独立的项目级任务，本轮不做
4. **决策点**：src strict 错误 ≤20 → TODO-01 提前到 Batch 1 直接开掉，后续所有批次享受 strict 保护；>20 → 留在 Batch 9 收官（下文按后者编排）

> **为何 strict 放最后**：strict 一开会把所有后续批次的 typecheck 验证污染成"混着存量错误"，无法区分新引入问题。正确顺序是先按 strict 标准修/写中间批次的代码，最后一批开开关、集中清剩余存量。

## Batch 1：纯配置批（TODO-13, 33, 41, 43半）——低风险热身

- **TODO-13**：`manifest.json` 加 `"action": { "default_icon": { "128": "src/assets/icon-128.png" } }`（@crxjs 会处理路径改写；仅有 128 一档图标，与 icons 字段保持一致）
- **TODO-33**：`vitest.config.ts` test 块加 `coverage: { provider: 'v8', reporter: ['text', 'html'], include: ['src/**'] }`；devDependencies 补 `@vitest/coverage-v8@^2`（与 vitest 2 同大版本）
- **TODO-41**：`eslint.config.js` L18-20 globals 改 `{ ...globals.browser, ...globals.serviceworker }`
- **TODO-43（半做）**：package.json 加 `"engines": { "node": ">=20.0.0" }`，version 0.0.0 → 0.1.0 对齐 manifest。**不加 `sideEffects: false`**——content/background 入口全是顶层副作用模块，该标记会诱导 bundler 摇掉入口逻辑，风险远大于包体收益
- 验证：常规 + 额外 `npm run build` 确认 crxjs 接受 manifest 变更 → commit

## Batch 2：sanitize 文件簇（TODO-02前半 + 30 + 08）

- **TODO-02 前半**（`src/lib/shared-types/sanitize.ts`）：SENSITIVE_KEYS 追加 `'apikey', 'api-key', 'api_key', 'x-api-key', 'x-goog-api-key', 'proxy-authorization'`。**保持精确匹配**，不改为 includes 模糊匹配（模糊会误伤 `apiKeyHint` 之类）；`config.apiKey` 经 toLowerCase 后即 `apikey` 可命中
- **TODO-30**：递归加 WeakSet 防循环——`sanitize(obj, seen = new WeakSet())`，进入对象/数组分支时 `if (seen.has(obj)) return '[Circular]'; seen.add(obj)`，递归透传 seen；seen 作默认参数保持导出签名兼容
- **TODO-08**：新建 `test/lib/shared-types/sanitize.test.ts`（`test/lib` 目录需新建）。用例：各敏感键脱敏（含大小写混合 `Authorization`/`ApiKey`）、嵌套对象、数组内对象、原始值/null/undefined 透传、非敏感键保留、循环引用不栈溢出返回 `[Circular]`、新增 apikey 变体全覆盖
- 备注：sanitize 当前运行时零调用，本批不强行接线调用方；Batch 9 的 error-boundary 脱敏（TODO-19）是其未来第一个消费场景的姊妹实现
- 验证 + commit

## Batch 3：致命 UI/内容脚本批（TODO-05, 06, 03, 04）

- **TODO-05**（`SettingsPanel.tsx:176-243`）：逐个 request 改**两段式**——第一段同步遍历 providers 收集 `patterns: string[]`（复用现有 `isBuiltInProviderOrigin`/`resolveOriginPattern` 过滤逻辑，保留各 continue 分支）；第二段 `patterns.length > 0` 时一次 `await chrome.permissions.request({ origins: patterns })`，拒绝/异常走现有 `setSaveError("需要授权访问该域名")` 路径。同步更新 `SettingsPanel.test.tsx`：permissions.request 断言从"多次调用"改为"单次调用且 origins 含全部 pattern"
- **TODO-06**（`useResizable.ts:47-48`）：两行常量改 `typeof window !== 'undefined' ? window.innerWidth : Number.MAX_SAFE_INTEGER`（高度同理）；81-82 行使用点不需要动
- **TODO-03**（`content/index.tsx`）：加 `window.addEventListener('pagehide', () => { observer?.disconnect(); observer = null })`；顺带加 `pageshow` 里 `startObserver()` 防 bfcache 恢复后 observer 缺失（ensureMounted 幂等，面板仍在 DOM 中）。不必 unmount root（页面即将销毁）
- **TODO-04**（`ErrorDisplay.tsx:16-19`）：**真实重发方案（用户已确认）**——`handleRetry` = 清错误 + 从 `state.messages` 反向 find 最后一条 `role==='user'` 消息 + `sendMessage(content)`；找不到 user 消息时退化为纯关闭。只动本组件（补取 `state` 与 `sendMessage`），零侵入 chat-context reducer；重复气泡等价"用户手动重发"，可接受
- 验证 + commit

## Batch 4：config 层批（TODO-18, 02后半, 12, 36, 38）

- **TODO-18**（`provider-factory.ts:7-25`）：switch 加 default 分支抛 `` Error(`Unsupported provider format: ${String((config as { format?: unknown }).format)}`) ``——TS 穷尽检查不影响 default 合法性，防运行时脏数据正是目的。`provider-factory.test.ts` 加脏 format 抛错用例
- **TODO-02 后半**（同文件 `validateProviderConfig`）：apiKey 加字符集校验 `/^[\x21-\x7E]+$/`（可打印 ASCII、无空白），不匹配 push `'API Key 含非法字符'`——防 header 换行注入。测试同步加用例
- **TODO-12**（`session-memory.ts:49-84`）：`updateSessionMemory` 的 `chrome.storage.session.set` 包 try-catch，catch 返回 false（契约达成）；`getSessionMemory` 的读取包 try-catch，失败返回空骨架（契约本就是"不存在返回骨架"，读失败同样降级）。`session-memory.test.ts` 加 set 抛异常 → false 用例
- **TODO-36**（`settings-migration.ts:102`）：默认回退分支改 `{ ...DEFAULT_SETTINGS, providers: DEFAULT_SETTINGS.providers.map(p => ({ ...p })) }`；**查所有回退默认的位置**（规则 1 与规则 4 两处都要看）
- **TODO-38🔵**（`origin-pattern.ts:109-123,154`）：`const BUILT_IN_HOSTNAMES = collectBuiltInHostnames()` 模块级缓存一次，`isBuiltInProviderOrigin` 改查缓存
- 验证 + commit

## Batch 5：background/stream 批（TODO-09, 24, 25, 40）

- **TODO-09**（`stream.ts:571-577`）：三处调用点改 `.catch()` 兜底，且行为各有差异：
  - `handleChatMessage`：catch 里 `postToPort(port, session, { type:'error', message: friendlyMessage(inferErrorCode(msg), msg), code })` **并补发 `{ type:'done' }`**（CS 侧靠 done 退出 loading）
  - `handleGenerateTitle`：回 `type:'error'`
  - `handleTestConnection`：**必须回 `{ type:'connection_result', ok:false, error }`** 而非 error（前端 12s 兜底虽在，即时回包体验更好）
  - 三个 catch 都先判 `session.disconnected` 直接 return，复用文件内既有模式
- **TODO-24**（`stream.ts:332`）：for-await 之前加运行时防御——stream 为空或 `typeof stream[Symbol.asyncIterator] !== 'function'` 时 `throw new Error('模型未返回可迭代流')`，抛出后落入既有 catch → error+done 路径
- **TODO-25**（`stream.ts:478-479`）：`Array.from(rawTitle).slice(0, TITLE_MAX_CHARS).join('')` 码点级切片防 surrogate pair 撕裂
- **TODO-40🔵**（`port-protocol.ts:96-101`）：401/403/429 检测改 `/\b401\b/.test(message)` 边界匹配（避免 `id=14012` 误判）；`port-protocol.test.ts` 补边界用例
- 验证（stream.test.ts 既有用例必须全绿）+ commit

## Batch 6：tools 批（可拆 6a/6b 两次 commit）

**6a 小修（TODO-10, 26, 45）**：
- **TODO-10**（`slang-understand.ts:189-201`）：`safeUpdateMemory` 照抄 `query-expand.ts:221-234` 的模式——`const existing = await memoryStore.get(); const merged = existing ? Array.from(new Set([...existing.triedKeywords, normalized])) : [normalized]; await memoryStore.update({ triedKeywords: merged })`。`slang-understand.test.ts` 加"不覆盖已有关键词"断言
- **TODO-26**（`query-expand.ts:104-109`）：`const [suggest, hot] = await Promise.all([safeCall(...), safeCall(...)])`（safeCall 已吞错，Promise.all 不会拒绝）
- **TODO-45**（`llm-json.ts:96-105`）：`splitSystemMessage` 改 filter 出全部 system，`system: systemMsgs.map(m => m.content).join('\n\n') || undefined`

**6b 并发/超时/SSRF（TODO-17, 27, 28, 29）**：
- **TODO-17**（`working-memory.ts:43-57`）：模块级 `const updateQueues = new Map<string, Promise<unknown>>()`；`update` 把 read-merge-write 闭包链到 `updateQueues.get(traceId) ?? Promise.resolve()` 之后（`.then(work, work)` 保证前序失败不断链），链尾写回 Map，`release` 时删除 key。四个静态方法签名不变。`working-memory.test.ts` 加并发 update 无 lost-update 用例（两次并发 update 不同字段，断言两字段都在）
- **TODO-27**（`llm-json.ts:117-155` + `analyze-covers.ts:282-302`）：所有 generateObject/generateText/streamText 调用加 `abortSignal: AbortSignal.timeout(30_000)`
- **TODO-28**（`video-rerank.ts:92-107`）：Promise.allSettled 改分批——`for (let i = 0; i < truncated.length; i += 5)` 每批 allSettled 收集进总结果数组，后续 tagsByBvid 逻辑不变。`video-rerank.test.ts` 加并发峰值断言（mock fetchVideoTags 记录 inflight 计数，断言峰值 ≤5）
- **TODO-29**（`analyze-covers.ts:196-208`）：加 `isAllowedCoverUrl(url)`——new URL 解析，`protocol === 'https:'` 且 hostname 全等或以 `.hdslb.com`/`.bilibili.com` 结尾；normalizePicUrl 之后校验，不通过 throw（落入调用方既有 try/catch）。同时该 fetch 加 `signal: AbortSignal.timeout(10_000)`。`analyze-covers.test.ts` 加 http:// 与外域拒绝用例
- 验证 + commit（×2）

## Batch 7：bilibili-client 批（TODO-11, 35, 34）——修复与测试同批闭环

- **TODO-11**：`hot.ts:30` / `tags.ts:37` / `suggest.ts:30` / `wbi.ts:136（fetchWbiKeys）` 各 fetch 加 `signal: AbortSignal.timeout(10_000)`（统一 10s 与仓库惯例 stream.ts:474 一致，不用审计原文的 15s）。**wbi.ts:109 fetchSigned 特殊处理**：因 wbiFetch 有 401 重试、同一外部 signal 会跨两次请求且计时耗损，应在 `fetchSigned` 内部每次调用合成超时——`init.signal` 未提供时补 `AbortSignal.timeout(10_000)`（每次重试独立计时）；search.ts 经 wbiFetch 透传自动获益，无需改
- **TODO-35**（`search.ts:119-121` + `tags.ts:81-83` buildHeaders）：加 `const SESSDATA_PATTERN = /^[A-Za-z0-9%,*_-]+$/`（SESSDATA 实际为 URL 编码态 token）；不匹配则**不附 Cookie 降级匿名搜索**（不抛错——脏 cookie 不应炸掉整个搜索），两文件同步
- **TODO-34**：新建两个测试文件，参照同目录 `hot.test.ts` 的 mock 风格：
  - `test/bilibili-client/wbi.test.ts`：利用现成 `__setWbiKeyCacheForTests` 钩子——签名参数排序 / mixin key 提取 / 401 触发重试 / -352 风控触发重试 / key 缓存复用
  - `test/bilibili-client/search.test.ts`：mock global fetch + 预置 key cache——正常映射 mapVideoCard / 非 200 抛 BilibiliNetworkError / SESSDATA 合法附 Cookie / 非法不附
- 验证（既有 hot/tags/suggest 测试回归——fetch mock 不校验第二参故 signal 无碍）+ commit

## Batch 8：测试补全批（TODO-07, 14, 16）

- **TODO-07**：整文件重写 `test/components/Panel-settings.test.tsx`——删掉 8 个伪用例，改用同目录 `SettingsPanel.test.tsx` 的成熟模式（testing-library `render(<Panel .../>)` + createMockPort + test/setup.ts 全局 chrome mock），真实断言：开设置后 `chrome.runtime.connect` 被调（name 含 settings）、SettingsPanel 收到非 null port、关设置后 `port.disconnect` 被调、connect 抛异常时渲染"加载设置中..."且不崩。保留文件头的 vi.mock settings 写法
- **TODO-14**：`test/background/stream.test.ts` 追加 `handleTestConnection` describe（文件已存在、mock 架子齐全，只需补 import）：成功回 `connection_result ok:true` / generateText 拒绝回 `ok:false + error` / `session.disconnected=true` 时不 post /（Batch 5 修复后）try 外异常也回 connection_result
- **TODO-16**：新建 `test/tools/ask-clarification.test.ts` 与 `test/tools/bilibili-search.test.ts`——两工具均为工厂函数注入依赖模式（参照 `slang-understand.test.ts` / `query-expand.test.ts` 写法），测 execute 正常路径、依赖抛错降级路径、working-memory 交互
- 验证 + commit

## Batch 9：🟡组件杂项 + strict 收官（TODO-15, 23, 19, 20, 22, 31, 37, 01）

- **TODO-15**（`TestConnectionButton.tsx:153`）：`port.postMessage` 包同步 try/catch，catch 里 `setStatus("fail") + setErrorMessage("连接已断开") + cleanup() + return`
- **TODO-23**（同文件，与 15 同 commit）：加 `useEffect(() => cleanup, [provider, cleanup])` 使 provider 变化时清旧 listener/timer
- **TODO-19**（`error-boundary.tsx:20`）：加 `redactText(msg)`——正则替换 `/sk-[A-Za-z0-9_-]{8,}/g`、`/Bearer\s+\S+/gi`、`/SESSDATA=[^;\s]+/gi` 为 `[REDACTED]`，getDerivedStateFromError 里过一遍
- **TODO-20**（`VideoCard.tsx:34-61`）：parseTitle 简化——保留 em 提取 split，其余分支一律 `part.replace(/<[^>]*>/g, '')` 纯文本（删除 dangerousTags 白名单循环；React 转义兜底不变）。既有用例回归，补 svg/img onerror 输入用例
- **TODO-22**（`chat-context.tsx:686` 附近）：把 `titleRequestedRef.add(conversationId)` 移到"连接可用"判定块内部、紧邻 postMessage（12s 兜底定时器已存在，保留）
- **TODO-31**（`save-orchestrator.ts:262-281`）：触发点 5 的 useEffect 加 `let cancelled = false` + cleanup `() => { cancelled = true }`，异步回调内 `if (cancelled) return`（对齐触发点 4 风格）
- **TODO-37**（`App.tsx` / `ChatInput.tsx` / `HistoryDropdown.tsx`）：所有裸 `chrome.storage.local.get/set` 补 `.catch(() => {})` 或 try/catch 降级（读失败用默认值，写失败静默）
- **TODO-01 收官**：`tsconfig.app.json` 与 `tsconfig.node.json` 的 `/* Linting */` 区块顶部各加 `"strict": true`，随后 `npm run typecheck` 清空全部存量错误（预期集中在 strictNullChecks 类可选链修补，逐文件机械修、不改行为）。若错误量 >100 且时间受限，可拆为独立后续批，先只开 `"strictNullChecks": true, "noImplicitAny": true` 两项核心
- 最终验证：`npm test && npm run typecheck && npm run lint && npm run build`（build 确认 crxjs 产物完整）→ commit（可拆 2-3 个：组件杂项一个、strict 一个）

---

## 明确跳过/降级清单（8 条）

| TODO | 级别 | 处置 | 理由 |
|---|---|---|---|
| 02-明文存储本体 | 🔴半 | 跳过 | MV3 无可行的本地加密（密钥同样落盘=伪安全）；chrome.storage.local 是平台标准做法；UI 已有明文风险提示（SettingsPanel `api-key-risk-hint`）。只做白名单+字符集校验部分 |
| 21 | 🟠 | 降级为 Batch 0 验证 | 验证性任务非修复；npm test/build 当前可跑说明兼容性事实成立；升级 vitest 3 是独立项目任务 |
| 32 CSP | 🟡 | 降级为目检 | MV3 平台强制默认 CSP（`script-src 'self'`）已不可放宽，显式声明纯文档化；web_accessible_resources 由 @crxjs 构建时自动生成，手写易与产物冲突。build 后目检 dist/manifest.json 一次 |
| 43-sideEffects | 🔵半 | 跳过 | content/background 入口全是顶层副作用模块，`sideEffects:false` 会诱导 tree-shaking 摇掉入口逻辑，风险远大于包体收益 |
| 39 内联样式 | 🔵 | 跳过 | 纯渲染微优化，100+ 行样式搬迁 churn 大、无实测性能问题 |
| 42 storage key 常量化 | 🔵 | 跳过 | 纯重构，零行为收益，触面 3+ 文件 |
| 44 MessageList memo | 🔵 | 跳过 | 流式渲染重构回归风险高（消息列表是核心 UI），无 profiling 证据支撑；留待有实测数据再做 |
| 其余🔵未列项 | 🔵 | 跳过 | 同上性价比逻辑 |

**覆盖统计**：🔴 8/8 修，🟠 12/13 修 + 1 降级验证，🟡 14/16 修（32 降级、其余全修），🔵 3/8 顺手修（38/40/41）。总计修复约 37 条，每批独立可验证可回滚。

---

## 批次依赖与验证矩阵

| 批次 | 内容 | TODO | 验证命令 | 依赖 |
|---|---|---|---|---|
| 0 | 基线+strict 评估 | 21 | test + typecheck + lint + `tsc --strict --noEmit` + `npm ls` | 无 |
| 1 | 纯配置 | 13, 33, 41, 43半 | test + typecheck + lint + **build** | Batch 0 基线绿 |
| 2 | sanitize 簇 | 02前半, 30, 08 | test + typecheck | 无 |
| 3 | 致命 UI | 05, 06, 03, 04 | test + typecheck | 无 |
| 4 | config 层 | 18, 02后半, 12, 36, 38 | test + typecheck | 无 |
| 5 | background/stream | 09, 24, 25, 40 | test（stream.test.ts 全绿）+ typecheck | 无 |
| 6a | tools 小修 | 10, 26, 45 | test + typecheck | 无 |
| 6b | tools 并发/超时 | 17, 27, 28, 29 | test + typecheck | 无 |
| 7 | bilibili-client | 11, 35, 34 | test（hot/tags/suggest 回归）+ typecheck | 无 |
| 8 | 测试补全 | 07, 14, 16 | test + typecheck | **Batch 5**（TODO-14 断言依赖 09 修复后行为） |
| 9 | 组件杂项+strict | 15, 23, 19, 20, 22, 31, 37, 01 | test + typecheck + lint + **build** | 全部前置批（strict 需最后开） |

## 关键文件清单

- `Main/src/background/stream.ts` — TODO-09/24/25 修复 + TODO-14 测试被测对象（Batch 5/8 核心）
- `Main/src/components/model-settings/SettingsPanel.tsx` — TODO-05 批量 permissions.request 改造（P0 阻断项）
- `Main/src/lib/shared-types/sanitize.ts` — TODO-02/30/08 三合一安全批
- `Main/tsconfig.app.json` + `Main/tsconfig.node.json` — TODO-01 strict 开关（决定全局批次顺序）
- `Main/test/components/model-settings/SettingsPanel.test.tsx` — TODO-07 重写伪测试的模式范本 + TODO-05 断言同步修改
