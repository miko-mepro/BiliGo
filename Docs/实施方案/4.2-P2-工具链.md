# 4.2 P2 - 工具链完整（L3 + L4 集成）

> 目标一句话：输入"yyds 的鬼畜视频"，走完 5 步工作流，返回重排后的视频列表
> 对应里程碑：M2（2.6 §M2）
> 设计依据：3.1 §5-§9/§13/§15、3.3 §1/§3-§8/§10、2.5 R10

---

## 1. 前置条件

- P1 三级门禁全绿，已提交
- G5 闸口在本阶段验证（见 §4.4）

## 2. 子对话任务卡

### 并行批次

```
批次 A（并行）: SA-4 bilibili-client补全  |  SA-5 数据文件与llm-json  |  SA-6 working-memory
批次 B（并行，依赖 A）: SB-3 slang+expand  |  SB-4 search+rerank  |  SB-5 clarification+covers
批次 C（依赖 B）: SC-1 stream.ts 工具注册与 insight/videos 推送集成
```

### SA-4: bilibili-client 补全

```
【任务】实现 src/lib/bilibili-client/{hot.ts, suggest.ts, tags.ts}
【设计依据】3.1 §14
【旧仓库参照】Backend/BiliAgent/packages/bilibili-client/src/{hot,suggest,tags}.ts
【硬性契约】searchHot(limit)/searchSuggest(term)/fetchVideoTags(bvid) 签名与 3.1 §14 一致
【自测要求】tsc -b；移植旧仓库对应单测
```

### SA-5: 数据文件 + LLM JSON 调用

```
【任务】复制 slang-dictionary.json / tag-categories.json 到 src/tools/；
        实现 src/tools/llm-json.ts（generateObject 封装 + parseJsonFromText 降级）
【设计依据】3.3 §8
【旧仓库参照】Backend/BiliAgent/packages/extension/src/agent-tools/{slang-dictionary.json, tag-categories.json, llm-json.ts}
【硬性契约】
  - 优先 generateObject({model, schema, messages})；provider 不支持时降级 generateText + parseJsonFromText
  - parseJsonFromText 容错规则与 3.3 §8.2 一致（去 markdown 代码块 + 首{尾}提取）
  - 任何失败返回 null，不抛异常
【自测要求】tsc -b + parseJsonFromText 单测
```

### SA-6: working-memory

```
【任务】实现 src/tools/working-memory.ts
【设计依据】3.1 §9（T12/T13）、2.5 R10
【旧仓库参照】Backend/BiliAgent/packages/extension/src/agent-tools/working-memory.ts
【硬性契约】chrome.storage.session，key 前缀 'working-memory:'，TTL=5min；
           字段 triedKeywords/rejectedBvids/failureReasons/clarificationCount
【自测要求】tsc -b + TTL 过期单测（fake timers）
```

### SB-3: slang_understand + query_expand

```
【任务】实现 src/tools/{slang-understand.ts, query-expand.ts}（tool() 工厂形式，接收 port/state/memoryStore 注入）
【设计依据】3.3 §3/§4；输出类型 3.1 §5/§6
【旧仓库参照】Backend/BiliAgent/packages/extension/src/agent-tools/{understand.ts, expand.ts}
【硬性契约】
  - slang：字典最长匹配优先；命中不调 LLM；LLM 失败 normalized=原文；空输入直通；写 triedKeywords
  - expand：searchSuggest/searchHot safeCall 容错；先去重后截断（8/5/3）；LLM 失败 fallback=seed；合并 triedKeywords
  - execute 内推送 insight(understanding/expansion)
【自测要求】实现 3.7 §4（6 用例）+ §5（6 用例）
```

### SB-4: bilibili_search + video_rerank

```
【任务】实现 src/tools/{bilibili-search.ts, video-rerank.ts}
【设计依据】3.3 §5/§6；输出类型 3.1 §4/§7
【硬性契约】
  - search：keyword 空抛 Error；getSessdata() 经 chrome.cookies；风控 -352 → BILIBILI_RISK；
           execute 内推送 videos；analyze_covers 默认 false
  - rerank：MAX_CANDIDATES=30 截断记 trimmed；fetchVideoTags Promise.allSettled 容错；
           score clamp[0,1]；未知 bvid 丢弃；fallback score=1-i*0.01；
           推送 insight(rerank) + videos(重排后)；失败写 failureReasons
【自测要求】实现 3.7 §6（8 用例）
```

### SB-5: ask_clarification + analyze_covers

```
【任务】实现 src/tools/{ask-clarification.ts, analyze-covers.ts}
【设计依据】3.3 §7；3.1 §8/§15
【旧仓库参照】Backend/BiliAgent/packages/extension/src/background/cover-analysis.ts
【硬性契约】
  - clarification：question 空抛 Error；clarificationCount+1；推送 insight(clarification)
  - covers：HARD_MAX_COVERS=5；7天缓存 key 'bili-agent-cover-analysis-cache'；
           vision 不支持缓存降级标记 '该模型不支持视觉'；失败不缓存
【自测要求】tsc -b + covers 缓存三态单测
```

### SC-1: stream.ts 工具集成

```
【任务】改写 src/background/stream.ts：注册 5 个 tool（slang/expand/search/rerank/clarification）
        + SYSTEM_PROMPT（3.3 §1.1 原样）+ 工具消息顺序保证
【设计依据】3.2 §5/§6/§9.2、3.3 §2
【硬性契约】
  - 工具消息顺序：tool_start → videos/insight（execute 内）→ tool_result
  - tool-error part → error 消息（含工具名上下文）后结束循环
  - analyze_covers 不注册为独立 tool，由 bilibili_search 参数触发
【自测要求】stream.test.ts 补充工具事件用例
```

## 3. 联调与验收

### 3.1 自动验收

```bash
npm run typecheck && npm run build && npm run test
```

新增：slang(6) + expand(6) + rerank(8) + 工具事件用例全绿。

### 3.2 手动验收（来自 2.6 M2）

| # | 步骤 | 预期 |
|---|------|------|
| 1 | 输入"yyds 的鬼畜视频" | 依次看到 5 个工具步骤消息（P2 阶段 UI 极简，看 Port 消息日志亦可） |
| 2 | slang 步骤 | insight: yyds → 永远的神，matchedDict=true |
| 3 | expand 步骤 | keywords/tags/categories 生成 |
| 4 | search 步骤 | 收到 videos 消息（10-30 条 BilibiliVideoCard） |
| 5 | rerank 步骤 | 收到 insight(rerank) + 重排后 videos |
| 6 | 最终回复 | 自然语言总结流式输出 |
| 7 | 输入"你好"（闲聊） | 不调用任何工具，直接回答 |
| 8 | 输入模糊意图（如"来点好看的"） | 触发 ask_clarification，且同会话第二次不再触发 |

### 3.3 风险验证点（G5 / R10）

- [ ] SW 手动停止再唤醒后，WorkingMemory.triedKeywords 仍在（storage.session 跨 SW 重启保留）
- [ ] 扩展重载后 WorkingMemory 清空（会话边界正确）
- 失败预案：改用 storage.local + TTL，并补清理逻辑

## 4. 阶段提交

```
feat(P2): 工具链完整——5步工作流闭环
- 7个 agent tool（tool() + zod inputSchema）+ 俚语/标签字典
- bilibili-client 补全 hot/suggest/tags
- llm-json 改用 generateObject（根因：手写 JSON 解析易碎，schema 校验前移）
- stream.ts 注册 tools + SYSTEM_PROMPT + 工具消息顺序保证
- 测试：+20 单测全绿；R10 会话边界已验证
```
