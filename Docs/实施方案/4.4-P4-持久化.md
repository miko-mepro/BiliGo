# 4.4 P4 - 持久化（L7）

> 目标一句话：刷新页面后对话还在；多标签页历史同步；标题自动生成
> 对应里程碑：M4（2.6 §M4）——**可与 P5 并行**
> 设计依据：3.4 §5-§8、3.1 §11、2.5 R12

---

## 1. 子对话任务卡

```
批次 A（并行）: SA-10 历史存储与同步  |  SA-11 保存编排器与会话记忆
批次 B（依赖 A）: SB-9 HistoryDropdown + ChatContext hydrate 接线 + 标题生成
```

### SA-10: 历史存储与跨标签页同步

```
【任务】移植 src/lib/history/{store.ts, sync.ts}
【设计依据】3.4 §5/§7；3.1 §11（T15）
【旧仓库参照】Backend/BiliAgent/packages/extension/src/content/history/{store.ts, sync.ts}
【硬性契约】
  - Key：'bili-agent-history-index' / 'bili-agent-history:{id}' / 'bili-agent-conversation'
  - MAX_HISTORY=50、MAX_PERSISTED_MESSAGES=200、ConversationData version:2
  - HistorySync syncId 机制：唯一 syncId + 200ms pending TTL + 自身写入忽略
【自测要求】移植 store.test.ts / sync.test.ts
```

### SA-11: 保存编排器 + 会话记忆

```
【任务】移植 src/lib/history/save-orchestrator.ts + src/config/session-memory.ts
【设计依据】3.4 §6
【硬性契约】4 个保存触发点：300ms 防抖 / HISTORY_SAVE_EVENT / beforeunload / 首次启用迁移
【自测要求】移植 save-orchestrator.test.ts / session-memory.test.ts（fake timers 验证防抖）
```

### SB-9: 历史 UI + hydrate + 标题生成

```
【任务】移植 src/components/HistoryDropdown.tsx；ChatContext 接通 HYDRATE/REHYDRATE；
        实现标题自动生成（AI SDK generateText 替换旧 lightweight-llm Port 通道）
【设计依据】3.4 §3.4/§8；3.5 §2.3
【硬性契约】
  - HYDRATE 防覆盖：已有消息/loading/streaming 时只置 hydrated=true
  - 标题：前 2 条消息 → generateText → 20 字内；失败用首条 user 消息前 20 字；titleFinal 置位
  - 历史操作：加载/删除/重命名（双击）/清空全部
【自测要求】chat-context.test.ts hydration 组用例 + I5/I9/I10 不变量用例
```

## 2. 验收

### 2.1 自动验收

`npm run typecheck && npm run build && npm run test`——history 系列单测全绿。

### 2.2 手动验收（来自 2.6 M4）

| # | 步骤 | 预期 |
|---|------|------|
| 1 | 发送 3 条消息后刷新页面 | 对话完整恢复（含视频/洞察卡） |
| 2 | 打开历史下拉 | 显示 1 条记录 |
| 3 | 对话结束后稍等 | 标题从"新对话"变为 LLM 摘要（≤20字） |
| 4 | 新开标签页 B 新建对话 | 标签页 A 历史下拉自动 +1 |
| 5 | 删除单条 / 双击重命名 / 清空全部 | 均生效且跨标签页同步 |
| 6 | 制造 51 条历史 | 索引截断为 50 |

### 2.3 风险复核

- [ ] R12：双标签页同时操作历史，无重复刷新/丢失（syncId 生效）

## 3. 阶段提交

```
feat(P4): 持久化——历史系统+跨标签页同步+标题自动生成
- 4触发点保存编排；syncId 防自身回调（根因：storage.onChanged 会被自身写入触发）
- 标题生成改 generateText（根因：旧 Port 通道已随双Port废弃）
```
