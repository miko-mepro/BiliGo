# 4.3 P3 - UI 完整（L5 + L6）

> 目标一句话：完整聊天界面——可拖拽面板、消息气泡、视频卡片、洞察卡片、主题切换
> 对应里程碑：M3（2.6 §M3）
> 设计依据：3.5 UI 线框图、3.4 §1-§4、2.5 R3/R6
> 特点：绝大部分组件为"原样移植"，子对话主要做复制+适配，风险集中在样式注入（G4）

---

## 1. 前置闸口 G4：样式注入验证（R3）

**主对话直接执行**（先于一切组件移植）：

1. 从旧仓库 `styles.ts` 截取任一组件的 CSS 片段
2. 经 `new CSSStyleSheet() + replaceSync + adoptedStyleSheets` 注入 Shadow DOM
3. Chrome 中该片段样式生效 → 通过
4. **失败预案**：降级为 `<style>` 标签注入（保持 Shadow DOM 隔离，仅体积增大）

## 2. 子对话任务卡

### 并行批次

```
批次 A（并行）: SA-7 hooks三件套  |  SA-8 样式移植  |  SA-9 工具函数
批次 B（并行，依赖 A）: SB-6 内容组件（消息类）  |  SB-7 内容组件（视频类）  |  SB-8 洞察卡片
批次 C（依赖 B）: SC-2 外壳组件（Panel/Toggle/App）  |  SC-3 ChatContext 完整接线
```

### SA-7: Hooks

```
【任务】移植 src/hooks/{useDraggable.ts, useResizable.ts, useTheme.ts}
【旧仓库参照】Backend/BiliAgent/packages/extension/src/content/hooks/
【硬性契约】原样移植（341/288/94 行）；useTheme 跨标签页同步保留；useStreamConsumer 不移植（废弃）
【自测要求】tsc -b；移植旧仓库 3 个 hook 单测
```

### SA-8: 样式

```
【任务】移植 src/content/styles.ts（1734 行 CSS）
【旧仓库参照】Backend/BiliAgent/packages/extension/src/content/styles.ts
【硬性契约】原样移植；注入方式按 G4 闸口结论（adoptedStyleSheets 或降级）
【自测要求】vite build 通过 + 构建产物含完整 CSS 文本
```

### SA-9: 工具函数

```
【任务】移植 src/utils/sort-filter.ts
【设计依据】3.5 §4.3（排序/时长/日期三维度规则）
【硬性契约】SortField/DateFilter/DurationFilter 枚举与解析规则原样
【自测要求】移植 sort-filter.test.ts
```

### SB-6: 消息类组件

```
【任务】移植 src/components/{ChatMessage.tsx, MessageList.tsx, ChatInput.tsx, ErrorDisplay.tsx}
【设计依据】3.5 §2/§3/§6
【硬性契约】
  - ChatMessage 五段结构：activity 指示器 / insight 卡 / steps 时间线 / reasoning 折叠 / Markdown 正文
  - ChatInput：Enter 发送、Shift+Enter 换行、loading 时变停止按钮（发 stop 消息）
  - ErrorDisplay 按 code 映射操作按钮（3.5 §6.2）
【自测要求】移植 MessageList.test.tsx 等对应单测
```

### SB-7: 视频类组件

```
【任务】移植 src/components/{VideoCard.tsx, FilterSortControls.tsx}
【设计依据】3.5 §4
【硬性契约】标题 <em> 高亮 + 危险标签过滤；播放量万/亿格式化；pic 补 https:；点击开新标签页
【自测要求】移植 VideoCard.test.tsx / FilterSortControls.test.tsx
```

### SB-8: 洞察卡片

```
【任务】移植 src/components/AgentInsightCard.tsx
【设计依据】3.5 §3.2；设计 Token 3.5 §7
【硬性契约】4 种 kind；clarification options 渲染可点击按钮（点击即作为用户消息发送）；可折叠
【自测要求】移植 AgentInsightCard.test.tsx
```

### SC-2: 外壳组件

```
【任务】移植 src/components/{Panel.tsx, ToggleButton.tsx, App.tsx}
【设计依据】3.5 §1/§9
【硬性契约】智能定位规则（3.5 §1.2）；min 280x300；8方向缩放；5px 拖拽阈值；
           点击外部关闭（isInteracting 时忽略）；面板开关跨标签页同步
【自测要求】tsc -b + 构建通过
```

### SC-3: ChatContext 完整接线

```
【任务】将 P1 的最小 chat-context.tsx 扩展为完整版，接通全部组件的 props/context
【设计依据】3.4 全文（19 Action reducer P1 已实现，此处主要是消费接线）
【硬性契约】10 个不变量（3.4 §10）不被破坏；Port 消息→Action 映射表（3.4 §4.1）完整实现
【自测要求】chat-context.test.ts 补全 Port 消费 + 不变量用例（3.7 §7.4/§7.5）
```

## 3. 验收

### 3.1 自动验收

`npm run typecheck && npm run build && npm run test`——新增组件/hook 单测全绿。

### 3.2 手动验收（来自 2.6 M3 + 3.5 §12）

| # | 步骤 | 预期 |
|---|------|------|
| 1 | 点击右下角 ToggleButton | 面板弹出，智能定位左/右 |
| 2 | 拖动面板到左上角 | 平滑跟随，5px 阈值内不触发 |
| 3 | 从 8 个方向缩放 | 最小 280x300 生效 |
| 4 | 输入"yyds 的视频" | 打字机回复 + 3 洞察卡（可折叠）+ 视频卡（标题高亮/播放量格式化）+ steps 时间线 |
| 5 | 排序/时长/日期筛选 | 视频列表即时变化 |
| 6 | clarification 场景 | options 按钮可点击并作为回复发送 |
| 7 | 切换深色模式 | 全 UI 变色；另一标签页同步 |
| 8 | 点击面板外部 | 关闭；拖拽中点击不误关 |
| 9 | user 气泡 | 右对齐 #FB7299 粉底白字 |

### 3.3 风险复核

- [ ] G4 R3：1734 行 CSS 完整生效（对照旧仓库截图走查主要视图）
- [ ] R6：保持 open 模式（已接受）

## 4. 阶段提交

```
feat(P3): UI完整——15组件+3hooks+1734行样式移植
- G4 闸口：adoptedStyleSheets 验证通过（或注明降级方案与根因）
- ChatContext 完整接线，10 不变量守恒
- 测试：组件/hook 单测全绿
```
