# 修复状态追踪

本文件统一追踪 11 个已确认问题和 5 个阶段工作的修复状态。状态定义、当前状态、阻塞关系和每阶段退出条件均在本文维护，阶段 README 不重复维护状态字段。

## 状态定义

| 状态 | 含义 | 是否阻塞后续 |
| --- | --- | --- |
| pending | 未开始，等待前置依赖完成或等待派工 | 阻塞依赖本项的后续任务 |
| in_progress | 正在实施修复或采集运行时证据 | 不视为完成，依赖本项的后续任务仍阻塞 |
| blocked | 遇到阻塞条件，无法继续推进 | 阻塞依赖本项的后续任务 |
| needs_review | 修复代码已提交，等待审查和测试 | 不阻塞独立后续任务，但阶段退出条件未满足 |
| completed | 修复已合并、测试通过、验收标准已达成 | 不阻塞任何后续任务 |

初始状态下，所有问题和阶段工作统一为 `pending`。本文档只记录计划状态，不记录已完成的修复结论。

## 当前状态表

### 问题状态

| 问题编号 | 严重程度 | 修复阶段 | 当前状态 | 前置依赖 | 备注 |
| --- | --- | --- | --- | --- | --- |
| R-1 | CRITICAL | 阶段1 | needs_review | 无 | 代码与测试已完成；E2E 冒烟验证依赖阶段0 |
| N-2 | HIGH | 阶段2 | pending | R-1 完成（建议） | 可与阶段0并行启动静态修复 |
| S-3 | HIGH | 阶段2 | needs_review | R-1 完成（建议） | 代码与测试已完成；E2E 冒烟验证依赖阶段0 |
| S-4 | HIGH | 阶段2 | pending | S-3 完成 | 强依赖已解除：S-3 批次模型（含 reranked 字段）已就位 |
| S-2 | HIGH | 阶段2 | pending | R-1 完成（建议） | 可与阶段0并行启动静态修复 |
| N-1 | MEDIUM | 阶段3 | pending | 阶段2 收敛 | N-4 的前置依赖 |
| N-4 | MEDIUM | 阶段3 | pending | N-1 完成 | 强依赖：N-4 需接入 N-1 定义的统一错误分类 |
| N-3 | MEDIUM | 阶段3 | pending | 阶段2 收敛 | 与 R-3 共用 TestConnectionButton 状态管理 |
| S-1 | MEDIUM | 阶段3 | pending | 阶段2 收敛 | S-1 到 S-4 因果链上游，建议与 S-2 协调 |
| R-2 | MEDIUM | 阶段3 | pending | 阶段2 收敛 | 与 R-3 同属异步竞态类，触发场景独立 |
| R-3 | MEDIUM | 阶段3 | pending | 阶段2 收敛 | 与 N-3 共用 TestConnectionButton 状态管理 |

### 阶段工作状态

| 阶段 | 名称 | 当前状态 | 前置依赖 | 备注 |
| --- | --- | --- | --- | --- |
| 阶段0 | 前置准备 | in_progress | 无 | E2E 挂载阻塞已解除（见下方备注），运行时证据采集通道未完成 |
| 阶段1 | 致命问题修复 | needs_review | 无硬依赖 | R-1 代码与测试完成，待阶段0 提供 E2E 冒烟验证 |
| 阶段2 | 高优先级修复 | in_progress | 阶段1 完成（建议） | S-3 已 needs_review；S-4/N-2/S-2 待开始 |

### 阶段0 当前进展备注

**已达成**（E2E 挂载阻塞解除）：

根因已定位——`--load-extension` 需**同时**满足两个条件才生效：
1. 持久化上下文（`launchPersistentContext`）：非持久化上下文是 incognito 语义，扩展默认不启用，content script 不注入、扩展 SW 不启动；
2. 完整 Chromium 二进制（`channel: "chromium"`）：Playwright 默认 headless 使用的 headless shell 不支持扩展加载。

对应改动：新增 `Main/e2e/fixtures/extension-fixture.ts`（覆写 context/page 为持久化上下文 + 每用例独立临时 profile）、新增 `Main/e2e/extension-probe.spec.ts`（最小启动探针）、`extension-harness.ts` 由「只等 toggle 一个选择器」改为三段探针（SW 启动 → `#bili-agent-host` attached → toggle visible）并附带脱敏证据采集、`playwright.config.ts` 补齐 channel 与扩展启动参数。

证据：`Main/test-results/.last-run.json` 为 `{"status":"passed","failedTests":[]}`。

**未达成**（运行时证据采集通道）：

`Main/.sisyphus/evidence/` 为空目录，SW 控制台日志、DevTools Network 记录、DOM 录制、`scrollIntoView` 调用计数四类采集能力均未建立，退出条件第 3、5 条未满足。该缺口阻塞阶段4 的假设验证（N-V1 到 N-V6、S-V1 到 S-V5），也阻塞阶段1 到阶段3 各问题从 `needs_review` 转 `completed`。

**待复核**：三段探针的实际运行输出未逐条留存，退出条件第 1、2、4 条为旁证达成而非直接复核。
| 阶段2 | 高优先级修复 | pending | 阶段1 完成（建议） | S-4 依赖 S-3 |
| 阶段3 | 中优先级修复 | pending | 阶段2 收敛 | N-4 依赖 N-1；N-3 与 R-3 需协调 |
| 阶段4 | 测试与验证 | pending | 阶段0 恢复 E2E 能力 + 阶段1到3 基本完成 | 需要运行时验证能力 |

## 阻塞关系

### 强阻塞（硬依赖）

以下依赖关系中，前置任务必须达到 `completed` 状态，后续任务才能从 `pending` 转为 `in_progress`：

1. **S-4 阻塞于 S-3**：S-4 的排序行为定义需要 S-3 建立的批次归属模型（`searchId`/`batchId`/`messageId`）作为锚点。S-3 未完成时启动 S-4 会导致排序重置和覆盖行为没有稳定的批次上下文，修复会反复返工。
2. **N-4 阻塞于 N-1**：N-4 需要把 Bilibili 工具网络失败接入 N-1 定义的统一网络错误分类语义层。N-1 未完成时启动 N-4 会缺少统一分类基础，工具调用路径与连接测试文案仍会不一致。
3. **阶段4 阻塞于阶段0**：阶段4 的运行时验证、E2E 场景回归和跨域集成验证依赖阶段0 恢复的 E2E 挂载能力。阶段0 未完成时阶段4 无法采集运行时证据。

### 协调关系（非硬阻塞）

以下关系不是硬阻塞，但相关问题的修复必须协调进行，避免分别修改共用代码造成二次竞态或不一致：

1. **N-3 与 R-3 共用 TestConnectionButton 状态管理**：两者共同涉及 `Main/src/components/model-settings/TestConnectionButton.tsx:115-189`。N-3 关注 Port 断线检测能力，R-3 关注异步 Promise 完成时机。修复时应统一检查 listener、timer、取消和过期结果，避免分别修改造成状态管理冲突。
2. **S-1 到 S-4 因果链**：搜索结果展示的四个问题形成逐级传导的因果链。S-1（即时展示）是上游，S-2（无条件滚动）、S-3（全局替换）、S-4（排序覆盖）逐级放大。S-1 归入阶段3，S-2/S-3/S-4 归入阶段2，修复 S-1 时需回查 S-2/S-3/S-4 的修复是否仍成立。
3. **R-2 与 R-3 同属异步竞态类**：两者触发场景独立（历史重命名 vs 连接测试），但修复模式相似（引入取消机制）。建议共享取消机制的设计经验，但不必互相等待。

### E2E 阻塞的特殊规则

阶段0 的 E2E 挂载阻塞遵循以下规则：

- E2E 挂载阻塞**不阻塞** R-1、N-2 等可由静态证据确认的修复。这类修复可以与阶段0 并行启动。
- E2E 挂载阻塞**阻塞** 最终运行时证据采集。R-1、R-2、R-3 的渲染崩溃堆栈、S-2 的实际滚动频率、S-4 的最终 DOM 顺序等运行时验证必须等待阶段0 恢复 E2E 能力后才能采集。
- 因此，阶段1 到阶段3 的代码修复可以先行推进，但其 `completed` 状态的最终验收需要阶段0 提供运行时证据支持。

## 每阶段退出条件

### 阶段0 退出条件

- E2E 测试夹具（`Main/e2e/fixtures/extension-harness.ts`）能稳定等待 content script 挂载，`[data-bili-agent-toggle]` 在限定时间内出现。
- `Channel closed` 和 `page.goto: Target page, context or browser has been closed` 错误已消除或已规避。
- 运行时证据采集通道（service worker 控制台、DevTools Network、DOM 录制）可用。
- 至少一条目标 smoke 测试能进入面板打开和按钮交互阶段。

### 阶段1 退出条件

- R-1 的历史记录数据入口校验已实现，非字符串 `title` 不再触发 `r.title.toLowerCase()` 抛出 `TypeError`。
- `store.ts` 和 `sync.ts` 的读取路径均经过结构完整性检查。
- 错误边界能捕获校验拒绝的异常数据并给出可读提示。
- 代码审查通过，单元测试覆盖非字符串 `title` 的拒绝路径。
- 运行时验证通道可用后，补充一次 E2E 冒烟验证渲染不再抛错（本条件可在阶段0 完成后补验）。

### 阶段2 退出条件

- N-2：聊天流 `streamText` 已设置独立总超时，参考连接测试的 10 秒和工具请求的 30 秒设定合理阈值。
- S-2：自动滚动 effect 引入用户阅读状态保护，用户上拉后暂停自动滚动，仅在新消息到达且用户已位于底部时恢复滚动。
- S-3：视频结果引入批次归属模型，旧视频能挂在旧输出下，新搜索不再直接覆盖旧数组。
- S-4：客户端排序在后端已重排时保留重排顺序或用户手动选择，不再无条件覆盖；视频数组引用变化时保留用户手动选择。
- S-4 的完成以 S-3 完成为前提。
- 代码审查通过，单元测试覆盖批次保留、滚动保护和排序优先级场景。
- 运行时验证通道可用后，补充 S-V2、S-V3、S-V4 假设的运行时证据采集（本条件可在阶段0 完成后补验）。

### 阶段3 退出条件

- N-1：统一网络错误分类语义已定义，区分 DNS 错误、TLS 错误、HTTP 状态码错误、超时和 Provider 配置错误。
- N-4：Bilibili 工具网络失败已接入 N-1 定义的统一错误分类，文案与连接测试语义一致。N-4 的完成以 N-1 完成为前提。
- N-3：设置 Port 已注册 `onDisconnect` 处理，统一两条 Port 链路的断线检测能力。
- S-1：评估辅助内容展示时机，引入"搜索输出块"概念或延迟到最终 `tool_result` 之后展示，并回查 S-2/S-3/S-4 修复是否仍成立。
- R-2：历史重命名引入取消机制（`AbortController` 或 `cancelled` 标志），异步操作在组件卸载或状态变化时取消未完成的 Promise。
- R-3：连接测试 `Promise.race` 引入取消机制，旧请求不再在视图切换后写入过期状态。N-3 与 R-3 的共用状态管理已统一检查。
- 代码审查通过，单元测试覆盖错误分类、断线检测、取消机制和状态过期场景。
- 运行时验证通道可用后，补充 N-V1 到 N-V6 假设的运行时证据采集（本条件可在阶段0 完成后补验）。

### 阶段4 退出条件

- 测试覆盖缺口清单的 7 项缺口均已补齐测试用例，或已明确产品决策后定义预期行为。
- E2E 场景回归覆盖渲染崩溃、自动滚动、批次保留、排序优先级、错误分类和断线检测。
- 跨域集成验证覆盖网络层错误分类与工具调用、状态管理与渲染层、数据入口校验与跨标签页同步的集成点。
- 运行时验证已采集 N-V1 到 N-V6、S-V1 到 S-V5 假设的最小证据，已确认的假设已从待验证类移至已确认问题分类。
- 所有阶段的运行时补验条件均已达成。

## 状态更新规则

- 状态变更时必须同步更新本文件的"当前状态表"。
- 阶段 README 不维护状态字段，只描述阶段目标、包含方案和退出条件。
- 状态从 `needs_review` 转 `completed` 时，必须满足对应阶段的退出条件。
- 运行时补验条件未达成时，问题状态可标记为 `needs_review`，但不能标记为 `completed`。
