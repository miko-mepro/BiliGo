# BiliGO BugFix 任务进度总览

> **快照日期**：2026-08-03<br>
> **快照基线**：`8c3395d`（生成本文前 `bugfix` 分支的 HEAD）<br>
> **正式状态源**：[`修复状态追踪.md`](./修复状态追踪.md)<br>
> **本文职责**：汇总执行进度和证据，不替代正式状态源，也不修改阶段方案中的计划状态。

## 当前定位

> **阶段2：高优先级修复收敛期。S-3 已完成实现和单元/组件测试，正在等待运行时验收与最终复核；N-2 和 S-2 尚未启动，可并行进入实施；S-4 需要等待 S-3 正式关闭，并先完成产品排序优先级决策。阶段3和阶段4尚未进入执行。**

当前定位依据如下：

- 阶段0已完成 E2E 挂载与运行时证据通道的实现，但状态仍为 `needs_review`。
- 阶段1的 R-1 已完成代码和单元测试，但仍缺针对脏历史标题的运行时补验和最终关闭。
- 阶段2只有 S-3 有实际执行日志；N-2、S-2、S-4 没有对应执行日志或测试提交记录。
- 阶段3依赖阶段2收敛；阶段4依赖阶段0的运行时能力以及阶段1至阶段3形成可回归对象。

## 统计摘要

### 核心问题任务

11个核心问题按问题编号统计，不把阶段0和阶段4基础设施工作混入问题完成率：

| 状态 | 数量 | 问题编号 | 是否计入正式完成 |
| --- | ---: | --- | --- |
| `completed` | 0 | 无 | 是 |
| `needs_review` | 2 | R-1、S-3 | 否，单独计为已执行待关闭 |
| `in_progress` | 0 | 无 | 否 |
| `pending` | 9 | N-2、S-2、S-4、N-1、N-3、N-4、S-1、R-2、R-3 | 否 |
| `blocked` | 0 | 无 | 否，当前没有登记为硬阻塞的任务 |

**核心问题正式完成率：`0/11`。已执行待关闭：`2/11`。**

### 阶段工作

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 阶段0 | `needs_review` | E2E能力和运行时证据通道已有执行记录，等待最终复核 |
| 阶段1 | `needs_review` | R-1代码、单测和相关回归记录已有，仍需运行时补验/复核 |
| 阶段2 | `needs_review` | S-3已有执行记录；N-2、S-2、S-4仍待启动 |
| 阶段3 | `pending` | 六个中优先级问题均未开始 |
| 阶段4 | `pending` | 测试缺口、E2E回归、跨域集成和运行时假设验证均未进入阶段4执行 |

**阶段正式完成率：`0/5`。`needs_review`：`3/5`。`pending`：`2/5`。**

### 阶段4专项计划

阶段4专项统计与方案目录保持独立，不因为阶段0或 S-3 已有局部测试而提前计为阶段4完成：

| 专项 | 计划数量 | 当前状态 |
| --- | ---: | --- |
| 测试覆盖缺口 | 7项 | `pending` |
| E2E回归类别 | 8类 | `pending` |
| 跨域集成点 | 5项 | `pending` |
| 运行时假设 | 11项（N-V1至N-V6、S-V1至S-V5） | `pending` |

## 已执行但未正式完成

以下内容有代码或测试执行证据，但按照正式状态源的退出条件，不能标记为 `completed`：

| 范围 | 已执行内容 | 当前仍缺少的条件 | 证据 |
| --- | --- | --- | --- |
| 阶段0 | 持久化 Chromium fixture、三段扩展探针、运行时证据采集、诊断字段统一和脱敏；typecheck、ESLint、探针测试及2个目标 smoke 通过 | 最终复核；MV3 service worker 回收假设 N-V3 未实测；另有2个不属于阶段0范围的 smoke 失败待单独处理 | [`Devlogs/阶段0-E2E测试环境.md`](./Devlogs/阶段0-E2E测试环境.md)，提交 `92ff39b`、`5925291`、`44addfb` |
| R-1 | 历史索引统一校验、store/sync入口接入、渲染层兜底；42个测试文件、702项测试通过，typecheck通过 | 脏 title E2E冒烟、脏数据来源运行时证据、最终复核；非法记录对应存储 key 的清理仍是已知取舍 | [`Devlogs/R-1-历史标题类型校验.md`](./Devlogs/R-1-历史标题类型校验.md)，提交 `6383f77` |
| S-3 | VideoBatch协议/状态/渲染/持久化模型、批次迁移和批次测试；43个测试文件、738项测试通过，typecheck通过 | 双搜索运行时证据、批次上限和跨标签页同步边界复核；最终复核 | [`Devlogs/S-3-视频批次归属模型.md`](./Devlogs/S-3-视频批次归属模型.md)，提交 `e697949` |

### 证据冲突

`修复状态追踪.md` 的 R-1 和 S-3 备注分别列出 `128d9e7`、`627ef7f` 的 E2E相关提交，但两份对应 Devlog仍明确写着 E2E冒烟未执行。当前快照不把这两个状态提升为 `completed`，也不把提交号直接视为运行时通过证据；后续需要核对提交内容和可复现的运行产物，再决定是否更新正式状态源。

## 阶段进度

| 阶段 | 计划范围 | 已执行内容 | 剩余验收 | 状态 | 下一动作 |
| --- | --- | --- | --- | --- | --- |
| 阶段0 前置准备 | 恢复扩展 E2E挂载、建立 service worker/Network/DOM/滚动证据通道 | 已完成 fixture、探针、harness诊断和脱敏；探针1项、目标 smoke 2项通过 | 最终复核；N-V3未验证；阶段0日志记录的2个既有 smoke 失败需保留边界 | `needs_review` | 核对运行产物与状态追踪中的E2E提交，再关闭阶段0或补验证 |
| 阶段1 致命问题修复 | 修复 R-1 历史标题类型校验缺失 | 入口清洗、同步校验、组件兜底和702项单测已执行 | 脏 title E2E冒烟、脏数据来源证据和最终复核 | `needs_review` | 使用阶段0通道补做R-1运行时冒烟并复核 `store.ts`/`sync.ts` 路径 |
| 阶段2 高优先级修复 | N-2、S-2、S-3、S-4 | S-3实现及738项单测已执行；S-3建立了S-4所需批次锚点 | N-2、S-2未启动；S-4产品优先级和实现未启动；S-3运行时验收未完成 | `needs_review` | 并行启动N-2/S-2；完成S-4产品决策后，在S-3正式关闭后实施S-4 |
| 阶段3 中优先级修复 | N-1、N-3、N-4、S-1、R-2、R-3 | 未发现对应Devlog、代码提交或测试执行证据 | 六项问题的代码、测试、复核和运行时验证 | `pending` | 等阶段2收敛后，先做N-1，再接入N-4；N-3/R-3协调修改 |
| 阶段4 测试与验证 | 7项测试缺口、8类E2E回归、5项跨域集成、11项运行时假设 | 未发现阶段4专项执行记录；阶段0和S-3的局部证据不等于阶段4验收 | 全部专项验收、产品决策、运行时证据和阶段1至3关闭 | `pending` | 等阶段0能力正式收口及阶段1至3形成可回归对象后执行 |

## 11个核心问题明细

| 编号 | 严重程度 | 阶段 | 当前状态 | 已执行/证据 | 剩余条件与下一步 |
| --- | --- | --- | --- | --- | --- |
| R-1 | CRITICAL | 阶段1 | `needs_review` | 入口校验、同步校验、渲染兜底；702项测试通过；提交 `6383f77` | E2E脏 title 冒烟、运行时来源证据、最终复核 |
| N-2 | HIGH | 阶段2 | `pending` | 未发现执行日志或修复提交 | 增加聊天流独立总超时、补超时错误测试，再采集N-V5/N-V6证据 |
| S-3 | HIGH | 阶段2 | `needs_review` | 批次模型四层实现；738项测试通过；提交 `e697949` | 双搜索DOM/消息时间线运行时证据；复核批次上限、跨标签页边界 |
| S-4 | HIGH | 阶段2 | `pending` | 仅有方案，未发现执行日志或修复提交 | 先确认rerank与用户手动排序优先级，再在S-3正式关闭后实施 |
| S-2 | HIGH | 阶段2 | `pending` | 仅有方案，未发现执行日志或修复提交 | 实现用户上拉保护，补滚动行为测试，采集S-V2证据 |
| N-1 | MEDIUM | 阶段3 | `pending` | 仅有方案，未发现执行日志或修复提交 | 定义DNS/TLS/HTTP/超时/Provider配置错误分类并补测试；作为N-4前置 |
| N-4 | MEDIUM | 阶段3 | `pending` | 仅有方案，未发现执行日志或修复提交 | 等N-1完成后接入Bilibili工具网络错误语义 |
| N-3 | MEDIUM | 阶段3 | `pending` | 仅有方案，未发现执行日志或修复提交 | 与R-3同批检查设置Port listener、timer、断线反馈和取消机制 |
| S-1 | MEDIUM | 阶段3 | `pending` | 仅有方案，未发现执行日志或修复提交 | 调整辅助内容展示时机，并回查S-2/S-3/S-4因果链 |
| R-2 | MEDIUM | 阶段3 | `pending` | 仅有方案，未发现执行日志或修复提交 | 为历史重命名增加取消机制，补卸载后异步完成测试 |
| R-3 | MEDIUM | 阶段3 | `pending` | 仅有方案，未发现执行日志或修复提交 | 与N-3协调连接测试取消和过期结果保护 |

## 阶段4专项明细

### 7项测试覆盖缺口

| 编号 | 缺口 | 关联修复 | 当前状态 |
| --- | --- | --- | --- |
| 1 | 用户上拉后继续流式输出不应自动滚动 | S-2 | `pending` |
| 2 | chunk、reasoning或视频更新时的 `scrollIntoView` 调用频率 | S-2 | `pending` |
| 3 | 两次搜索结果是否按消息批次同时保留 | S-3 | `pending`，需产品决策 |
| 4 | 旧视频与旧 insight 的归属是否一致 | S-3 | `pending`，需产品决策 |
| 5 | rerank数组经过 `MessageList` 后的最终DOM顺序 | S-4 | `pending` |
| 6 | rerank顺序与默认播放量排序冲突时的优先级 | S-4 | `pending`，需产品决策 |
| 7 | 视频数组更新后是否重置用户手动筛选/排序 | S-4 | `pending`，需产品决策 |

S-3 Devlog中的批次测试证明了部分问题级契约，但没有替代阶段4要求的完整缺口验收；阶段4仍需按产品决策补齐端到端串联和验收记录。

### 8类E2E回归

| 类别 | 覆盖问题 | 当前状态 | 主要前置条件 |
| --- | --- | --- | --- |
| 渲染崩溃回归 | R-1、R-2、R-3 | `pending` | 阶段0、阶段1和阶段3 |
| 自动滚动回归 | S-2 | `pending` | 阶段0和阶段2 |
| 批次保留回归 | S-3 | `pending` | 阶段0和阶段2 |
| 排序优先级回归 | S-4 | `pending` | 阶段0和阶段2及产品决策 |
| 辅助内容时机回归 | S-1 | `pending` | 阶段0和阶段3 |
| 聊天流总超时回归 | N-2 | `pending` | 阶段0和阶段2 |
| 错误分类回归 | N-1、N-4 | `pending` | 阶段0和阶段3 |
| 断线检测回归 | N-3 | `pending` | 阶段0和阶段3 |

阶段4方案另有2项阶段0前置验证：扩展挂载能力和 smoke 面板交互。它们属于阶段0执行证据，不重复计入上述8类业务回归。

### 5项跨域集成点

| 编号 | 集成点 | 关联问题/范围 | 当前状态 |
| --- | --- | --- | --- |
| 1 | 网络错误分类与工具调用 | N-1、N-4 | `pending` |
| 2 | 状态管理与渲染层 | S-3、R-1 | `pending` |
| 3 | `TestConnectionButton` 状态管理 | N-3、R-3 | `pending` |
| 4 | 搜索展示因果链 | S-1、S-2、S-3、S-4 | `pending` |
| 5 | 数据生命周期一致性 | S-3、insight追加策略 | `pending` |

### 11项运行时假设

| 类别 | 编号范围 | 验证重点 | 当前状态 |
| --- | --- | --- | --- |
| 网络层 | N-V1至N-V6，共6项 | Provider/请求来源、Network状态、service worker收包与回收、Port断线、耗时、chunk/error/done | `pending` |
| 搜索UI | S-V1至S-V5，共5项 | Search触发区域、消息/滚动顺序、双搜索DOM批次、rerank与播放量顺序、候选字段完整性 | `pending` |

## 依赖与阻塞

### 强依赖

1. **S-4依赖S-3**：S-4的排序行为需要S-3提供稳定的批次上下文。S-3虽然已有实现，但仍为 `needs_review`，因此S-4尚未正式解除依赖。
2. **N-4依赖N-1**：N-1先定义统一网络错误分类，N-4再接入Bilibili工具调用路径。
3. **阶段4依赖阶段0**：阶段4的运行时验证、E2E回归和跨域集成依赖阶段0的E2E挂载及证据采集通道。

### 协调关系

- **N-3与R-3**共用 `TestConnectionButton` 状态管理，需统一检查listener、timer、取消和过期结果。
- **S-1到S-4**形成搜索展示因果链，S-1实施后必须回查S-2、S-3、S-4。
- **R-2与R-3**都属于异步竞态问题，可共享取消机制设计经验，但没有互相硬阻塞。

### 产品和证据阻塞

- 测试缺口3、4、6、7需要先明确批次保留、视频与 insight 归属、rerank优先级和用户筛选保留策略。
- R-1、S-3和阶段0的Devlog与状态追踪中关于E2E提交的描述不一致，需要先核对提交内容和可复现运行产物。
- 阶段0日志记录的MV3 service worker回收风险（N-V3）仍未实测；这会影响后续长时间运行时验证的可信度。
- 阶段0日志中的2个 smoke失败已被标记为既有业务测试缺陷，不应伪装成阶段0全部测试通过；需要单独决定修复或归档边界。

## 下一步执行顺序

1. 对账阶段0、R-1、S-3的状态追踪备注与对应提交，保留可复现运行产物后完成最终复核。
2. 并行实施N-2和S-2，并为S-4完成产品排序优先级决策；S-3正式关闭后再实施S-4。
3. 阶段3先实施N-1，再接入N-4；N-3与R-3作为同一状态管理批次协调；S-1、R-2可在依赖满足后并行推进。
4. 阶段1至阶段3形成可回归对象、阶段0正式收口后，按顺序执行阶段4的7项测试缺口、8类E2E回归、5项跨域集成和11项运行时假设验证。
5. 每个问题或阶段从 `needs_review` 转为 `completed` 前，必须同步更新 [`修复状态追踪.md`](./修复状态追踪.md)，并满足对应退出条件。

## 统计一致性检查

| 检查项 | 结果 |
| --- | --- |
| 11个问题编号均出现且只归属于一个核心修复阶段 | 通过：R-1、N-2、S-2、S-3、S-4、N-1、N-3、N-4、S-1、R-2、R-3各出现一次 |
| 阶段0至阶段4均有状态记录 | 通过：5个阶段均在阶段进度表出现 |
| 阶段4专项数量为7、8、5、11 | 通过：摘要和专项明细一致 |
| `needs_review`未计入正式完成率 | 通过：核心问题为 `0/11`，阶段为 `0/5` |
| R-1、S-3和阶段0缺口与Devlog一致 | 通过：运行时补验、最终复核和N-V3缺口均保留；提交号冲突单列待复核 |
| 当前定位与状态追踪依赖一致 | 通过：S-4依赖S-3、N-4依赖N-1、阶段4依赖阶段0均保留 |
| 原 `修复状态追踪.md`未被覆盖或改写 | 通过：本文为新增快照，不修改正式状态源 |
