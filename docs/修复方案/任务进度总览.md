# BiliGO BugFix 任务进度总览

> **快照日期**：2026-08-04<br>
> **快照基线**：`709157d`（生成本文前 `bugfix` 分支的 HEAD）<br>
> **正式状态源**：[`修复状态追踪.md`](./修复状态追踪.md)<br>
> **本文职责**：汇总执行进度和证据，不替代正式状态源，也不修改阶段方案中的计划状态。

## TODO列表

> 复选框表示执行动作是否已经发生，不等同于正式状态 `completed`。`[x]` 项仍可能处于 `needs_review`。

### 已完成执行

- [x] 阶段0：恢复 E2E 挂载、运行时证据采集和诊断脱敏能力（`completed`）。
- [x] R-1：完成历史标题入口校验、同步校验、渲染兜底及单元测试（`completed`）。
- [x] N-2：完成聊天流总超时、取消原因区分和流测试（`needs_review`）。
- [x] S-2：完成用户阅读保护、滚动节流和组件测试（`needs_review`）。
- [x] S-3：完成视频批次模型、协议/状态/渲染/持久化改动及单元/组件测试（`completed`）。
- [x] S-4：完成智能排序、用户手动排序保留和排序回归测试（`needs_review`）。
- [x] S-1：完成 A1 辅助内容延后到 `tool_result` 之后、五个工具顺序单测和 S-2/S-3/S-4 静态回查（`needs_review`）。

### 待处理

- [x] 对账阶段0、R-1、S-3 的 Devlog、状态追踪备注、相关提交和可复现运行产物。
- [x] 完成阶段0最终复核；N-V3 已完成代码分析并标记为部分确认风险，长延迟场景仍待专门运行时验证；2个既有 smoke 保留为范围外业务边界。
- [x] 为 R-1 补做脏 `title` E2E 冒烟并核验运行产物，满足阶段1关闭条件；脏数据实际来源仍待调查。
- [x] 实施 N-2 聊天流独立总超时并补测试；采集 N-V5/N-V6 运行时证据仍待阶段0通道。
- [x] 实施 S-2 用户阅读保护和滚动频率控制并补测试；采集 S-V2 运行时证据仍待阶段0通道。
- [x] 明确 S-4 采用策略 C（未手动选择时智能排序、用户手动排序优先）并完成实现；S-V4 运行时证据仍待阶段0通道。
- [x] N-1 统一网络错误分类已实施（`7832f0f`，`needs_review`）；N-4 工具网络错误语义已接入（`012c426`，`needs_review`）；运行时错误类型证据（N-V1）待阶段0通道。
- [x] N-3 与 R-3 已协调实施 `TestConnectionButton` 的 `onDisconnect`、请求级 cleanup 和取消机制（`2680be2`，`needs_review`）；E2E 断线/渲染证据待阶段0通道。
- [x] R-2 历史重命名卸载取消标记已实施（`3f8c6d8`，`needs_review`）；20 个组件测试通过；E2E 渲染证据待阶段0通道。
- [ ] 补齐阶段4的7项测试覆盖缺口，并为缺口3、4、6、7记录产品决策。
- [ ] 执行阶段4的8类 E2E 回归，保留错误边界、Port 和消息时间线证据。
- [ ] 执行阶段4的5项跨域集成验证，确认共享状态和数据生命周期没有冲突。
- [ ] 采集阶段4的11项运行时假设证据，并同步更新问题档案统计。
- [ ] 每个问题和阶段满足退出条件后，同步更新 [`修复状态追踪.md`](./修复状态追踪.md) 的正式状态。

## 当前定位（按5个阶段文件夹）

> **阶段0、阶段1已正式完成；阶段2仍为 `needs_review`，阶段3为 `in_progress`，阶段4仍为 `pending`。阶段2内 S-3 已完成双搜索 E2E 核验，N-2、S-2、S-4 仍等待各自运行时证据；阶段3六项问题仍等待浏览器运行时补验。**

当前定位依据如下：

- 阶段0的 E2E 挂载与运行时证据通道已完成最终复核；阶段1的 R-1 已完成脏标题 E2E 回归并正式关闭。
- 阶段2的 S-3 已完成双搜索批次保留 E2E 核验并正式关闭；N-2、S-2、S-4 的运行时证据仍未补齐。
- 阶段3已进入实施：N-1、N-3、N-4、S-1、R-2、R-3 六项均已完成代码与单元/组件测试并进入 `needs_review`，浏览器运行时证据仍待阶段0通道补验；阶段4的阶段0硬阻塞已解除，但完整执行仍需阶段2、阶段3形成可回归对象。

## 统计摘要

### 核心问题任务

11个核心问题按问题编号统计，不把阶段0和阶段4基础设施工作混入问题完成率：

| 状态 | 数量 | 问题编号 | 是否计入正式完成 |
| --- | ---: | --- | --- |
| `completed` | 2 | R-1、S-3 | 是 |
| `needs_review` | 9 | N-2、S-4、S-2、N-1、N-4、N-3、S-1、R-2、R-3 | 否，单独计为已执行待关闭 |
| `in_progress` | 0 | 无 | 否 |
| `pending` | 0 | 无 | 否 |
| `blocked` | 0 | 无 | 否，当前没有登记为硬阻塞的任务 |

**核心问题正式完成率：`2/11`。已执行待关闭：`9/11`。**

### 阶段工作

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 阶段0 | `completed` | E2E能力、运行时证据通道和 AC1-AC8 已完成最终复核 |
| 阶段1 | `completed` | R-1代码、单测、脏标题 E2E 回归和运行产物均已核验 |
| 阶段2 | `needs_review` | S-3 已完成双搜索 E2E 核验；N-2、S-2、S-4 仍待运行时补验 |
| 阶段3 | `in_progress` | N-1、N-3、N-4、S-1、R-2、R-3 六项均已完成代码与单元/组件测试并进入 `needs_review`；六项仍有浏览器运行时证据待阶段0通道补验 |
| 阶段4 | `pending` | 阶段0阻塞已解除；测试缺口部分已执行，E2E回归、跨域集成和运行时假设验证仍待执行 |

**阶段正式完成率：`2/5`。`needs_review`：`1/5`。`in_progress`：`1/5`。`pending`：`1/5`。**

### 阶段4专项计划

阶段4专项统计与方案目录保持独立，不因为阶段0或 S-3 已有局部测试而提前计为阶段4完成：

| 专项 | 计划数量 | 当前状态 |
| --- | ---: | --- |
| 测试覆盖缺口 | 7项 | `pending` |
| E2E回归类别 | 8类 | `pending` |
| 跨域集成点 | 5项 | `pending` |
| 运行时假设 | 11项（N-V1至N-V6、S-V1至S-V5） | `pending` |

## 执行证据与剩余条件

以下内容汇总已执行证据和仍需补验的边界；正式状态以 `修复状态追踪.md` 为准：

| 范围 | 已执行内容 | 当前仍缺少的条件 | 证据 |
| --- | --- | --- | --- |
| 阶段0 | 持久化 Chromium fixture、三段扩展探针、运行时证据采集、诊断字段统一和脱敏；typecheck、ESLint、探针测试及2个目标 smoke 通过 | N-V3 长延迟场景仍需专项运行时验证；2个业务 smoke 保留为阶段0范围外边界 | [`Devlogs/阶段0-E2E测试环境.md`](./Devlogs/阶段0-E2E测试环境.md)，提交 `92ff39b`、`5925291`、`44addfb` |
| R-1 | 历史索引统一校验、store/sync入口接入、渲染层兜底；42个测试文件、702项测试通过，typecheck通过；脏 title E2E 运行产物已核验 | 脏数据来源和非法记录对应存储 key 的清理仍是已知取舍 | [`Devlogs/R-1-历史标题类型校验.md`](./Devlogs/R-1-历史标题类型校验.md)，提交 `6383f77`、`128d9e7` |
| N-2 | 聊天流总超时、手动/断线/超时取消原因区分、兼容信号合并；39个流测试通过，typecheck通过 | 45秒阈值需运行时校准；超时 UI E2E和 N-V5/N-V6 证据待补验 | 本轮 `Main/src/background/stream.ts` 与 `test/background/stream.test.ts` |
| S-2 | 滚动容器底部判定、用户上拉保护、100ms节流；16个 MessageList 组件测试通过 | 实际滚动频率、动画叠加和上拉 E2E（S-V2）待补验 | 本轮 `Main/src/components/MessageList.tsx` 与 `test/components/MessageList.test.tsx` |
| S-3 | VideoBatch协议/状态/渲染/持久化模型、批次迁移和批次测试；43个测试文件、738项测试通过，typecheck通过；双搜索运行产物已核验 | 批次数量上限和跨标签页同步属于后续边界风险 | [`Devlogs/S-3-视频批次归属模型.md`](./Devlogs/S-3-视频批次归属模型.md)，提交 `e697949`、`627ef7f` |
| S-4 | `rerank` 智能排序、用户手动排序覆盖和同批次状态保留；排序/控件/MessageList 回归通过 | 产品策略已按 C 落地；最终 DOM 顺序和 S-V4 E2E 证据待补验 | 本轮 `Main/src/utils/sort-filter.ts`、`FilterSortControls.tsx`、`MessageList.tsx` 及对应测试 |
| N-1 | 统一错误分类已提交（`7832f0f`），单元测试通过 | 运行时错误类型证据待阶段0通道 | 提交 `7832f0f` |
| N-4 | tool-error 已接入统一网络/业务错误语义；本轮测试通过 | 运行时 `part.error` 形态和 N-V1 证据待阶段0通道 | 提交 `012c426` |
| N-3 | `onDisconnect`、请求级 cleanup 和断线测试已提交（`2680be2`） | E2E 断线时间证据待阶段0通道 | 提交 `2680be2` |
| R-2 | 卸载取消标记已提交（`3f8c6d8`），20 个组件测试通过 | E2E 渲染证据待阶段0通道 | 提交 `3f8c6d8` |
| R-3 | 请求代次和取消 cleanup 已提交（`2680be2`） | E2E 渲染证据待阶段0通道 | 提交 `2680be2` |
| S-1 | A1 辅助内容延后到 `tool_result` 之后、五个工具顺序单测、S-2/S-3/S-4 静态回查完成 | 浏览器运行时/E2E 证据待阶段0通道补验；A1 不提供原子 UI 更新 | 本轮 `Main/src/background/stream.ts` 与 `test/background/stream.test.ts` |

### 证据冲突

`修复状态追踪.md` 的 R-1 和 S-3 备注曾列出 `128d9e7`、`627ef7f` 的 E2E相关提交，而旧版 Devlog 曾写着 E2E冒烟未执行。本次已核对提交内容、E2E测试代码和 `Main/.sisyphus/evidence/runtime/` 运行产物，确认两项 E2E 均已执行；对应 Devlog和正式状态源已同步更新。运行产物被 `.gitignore` 排除，后续验收仍需重新采集新鲜证据。

## 阶段进度

| 阶段 | 计划范围 | 已执行内容 | 剩余验收 | 状态 | 下一动作 |
| --- | --- | --- | --- | --- | --- |
| 阶段0 前置准备 | 恢复扩展 E2E挂载、建立 service worker/Network/DOM/滚动证据通道 | fixture、探针、harness诊断和脱敏已完成；探针1项、目标 smoke 2项通过 | N-V3长延迟场景仍需专项运行时验证；2个业务 smoke 保留边界 | `completed` | 进入阶段4运行时验证前重新采集新鲜证据 |
| 阶段1 致命问题修复 | 修复 R-1 历史标题类型校验缺失 | 入口清洗、同步校验、组件兜底、702项单测和脏 title E2E 已执行 | 脏数据来源仍未确认；非法记录存储 key 清理为已知取舍 | `completed` | 后续仅在运行时调查脏数据来源时补证据 |
| 阶段2 高优先级修复 | N-2、S-2、S-3、S-4 | S-3批次模型和双搜索 E2E 已完成；其余代码与单元/组件测试已执行 | N-V5/N-V6、S-V2、S-V4和N-2/S-2/S-4最终E2E复核未完成 | `needs_review` | 使用阶段0通道补运行时证据，再进行阶段2最终复核 |
| 阶段3 中优先级修复 | N-1、N-3、N-4、S-1、R-2、R-3 | 六项代码及单元/组件测试均已执行并进入 `needs_review` | 浏览器运行时证据（N-V1/N-V3 等）和最终复核待阶段0通道补验 | `in_progress` | 使用阶段0通道补运行时证据，再进行阶段3最终复核 |
| 阶段4 测试与验证 | 7项测试缺口、8类E2E回归、5项跨域集成、11项运行时假设 | 缺口1/2/5测试已补齐，4项产品决策已采纳；其余专项尚未执行 | 缺口3/4/6/7测试、8类E2E、5项集成和11项假设证据 | `pending` | 阶段2/3运行时证据齐备后继续执行阶段4专项 |

## 11个核心问题明细

| 编号 | 严重程度 | 阶段 | 当前状态 | 已执行/证据 | 剩余条件与下一步 |
| --- | --- | --- | --- | --- | --- |
| R-1 | CRITICAL | 阶段1 | `completed` | 入口校验、同步校验、渲染兜底；702项测试通过；脏 title E2E 运行产物已核验；提交 `6383f77`、`128d9e7` | 脏数据来源和非法记录存储 key 清理仍是已知取舍 |
| N-2 | HIGH | 阶段2 | `needs_review` | 总超时、取消原因和37个流测试已执行 | 45秒阈值校准、超时UI E2E和N-V5/N-V6证据 |
| S-3 | HIGH | 阶段2 | `completed` | 批次模型四层实现；738项测试通过；双搜索 E2E 运行产物已核验；提交 `e697949`、`627ef7f` | 批次数量上限和跨标签页同步属于后续边界风险 |
| S-4 | HIGH | 阶段2 | `needs_review` | 策略C、智能排序和用户排序保留已执行，排序/组件测试通过 | 最终DOM顺序和S-V4证据；运行时复核 |
| S-2 | HIGH | 阶段2 | `needs_review` | 上拉保护、滚动节流和16个组件测试已执行 | 实际滚动频率和S-V2证据；运行时复核 |
| N-1 | MEDIUM | 阶段3 | `needs_review` | 统一错误分类已提交（`7832f0f`），单元测试通过 | 运行时错误类型证据待阶段0通道 |
| N-4 | MEDIUM | 阶段3 | `needs_review` | tool-error 已接入统一网络/业务错误语义；本轮测试通过 | 运行时 `part.error` 形态和 N-V1 证据待阶段0通道 |
| N-3 | MEDIUM | 阶段3 | `needs_review` | `onDisconnect`、请求级 cleanup 和断线测试已提交（`2680be2`） | E2E 断线时间证据待阶段0通道 |
| S-1 | MEDIUM | 阶段3 | `needs_review` | A1（tool_result 先于辅助内容）已实现；五个工具顺序单测覆盖；S-2/S-3/S-4 静态回查完成 | 浏览器运行时/E2E 证据待阶段0通道补验；A1 不提供原子 UI 更新 |
| R-2 | MEDIUM | 阶段3 | `needs_review` | 卸载取消标记已提交（`3f8c6d8`），20 个组件测试通过 | E2E 渲染证据待阶段0通道 |
| R-3 | MEDIUM | 阶段3 | `needs_review` | 请求代次和取消 cleanup 已提交（`2680be2`） | E2E 渲染证据待阶段0通道 |

## 阶段4专项明细

### 7项测试覆盖缺口

| 编号 | 缺口 | 关联修复 | 当前状态 |
| --- | --- | --- | --- |
| 1 | 用户上拉后继续流式输出不应自动滚动 | S-2 | `completed` |
| 2 | chunk、reasoning或视频更新时的 `scrollIntoView` 调用频率 | S-2 | `completed` |
| 3 | 两次搜索结果是否按消息批次同时保留 | S-3 | `pending`，已采纳永久保留批次方案，测试待执行 |
| 4 | 旧视频与旧 insight 的归属是否一致 | S-3 | `pending`，已采纳时间交错方案，测试待执行 |
| 5 | rerank数组经过 `MessageList` 后的最终DOM顺序 | S-4 | `completed` |
| 6 | rerank顺序与默认播放量排序冲突时的优先级 | S-4 | `pending`，已采纳策略C，测试待执行 |
| 7 | 视频数组更新后是否重置用户手动筛选/排序 | S-4 | `pending`，已采纳批次独立状态方案，测试待执行 |

S-3 Devlog中的批次测试证明了部分问题级契约，但没有替代阶段4要求的完整缺口验收；阶段4仍需按已采纳产品方案补齐端到端串联和验收记录。

### 8类E2E回归

| 类别 | 覆盖问题 | 当前状态 | 主要前置条件 |
| --- | --- | --- | --- |
| 渲染崩溃回归 | R-1、R-2、R-3 | `pending` | 阶段0、阶段1和阶段3 |
| 自动滚动回归 | S-2 | `pending` | 阶段0和阶段2 |
| 批次保留回归 | S-3 | `pending` | 阶段0和阶段2 |
| 排序优先级回归 | S-4 | `pending` | 阶段0和阶段2及产品决策 |
| 辅助内容时机回归 | S-1 | `pending` | 阶段0和阶段3 |
| 聊天流总超时回归 | N-2 | `pending` | 阶段0和阶段2 |
| 错误分类回归 | N-1、N-4 | `pending` | 阶段0和阶段3 |
| 断线检测回归 | N-3 | `pending` | 阶段0和阶段3 |

阶段4方案另有2项阶段0前置验证：扩展挂载能力和 smoke 面板交互。它们属于阶段0执行证据，不重复计入上述8类业务回归。

### 5项跨域集成点

| 编号 | 集成点 | 关联问题/范围 | 当前状态 |
| --- | --- | --- | --- |
| 1 | 网络错误分类与工具调用 | N-1、N-4 | `pending` |
| 2 | 状态管理与渲染层 | S-3、R-1 | `pending` |
| 3 | `TestConnectionButton` 状态管理 | N-3、R-3 | `pending` |
| 4 | 搜索展示因果链 | S-1、S-2、S-3、S-4 | `pending` |
| 5 | 数据生命周期一致性 | S-3、insight追加策略 | `pending` |

### 11项运行时假设

| 类别 | 编号范围 | 验证重点 | 当前状态 |
| --- | --- | --- | --- |
| 网络层 | N-V1至N-V6，共6项 | Provider/请求来源、Network状态、service worker收包与回收、Port断线、耗时、chunk/error/done | `pending` |
| 搜索UI | S-V1至S-V5，共5项 | Search触发区域、消息/滚动顺序、双搜索DOM批次、rerank与播放量顺序、候选字段完整性 | `pending` |

## 依赖与阻塞

### 强依赖

1. **S-4依赖S-3**：S-4的排序行为需要S-3提供稳定的批次上下文。S-3已完成实现和双搜索运行时核验，代码模型依赖已解除；S-4仍需自身运行时证据。
2. **N-4依赖N-1**：N-1先定义统一网络错误分类，N-4再接入Bilibili工具调用路径。
3. **阶段4依赖阶段0及阶段1到3收敛**：阶段0的E2E挂载及证据采集通道已完成；阶段4仍需等待阶段2、阶段3形成完整回归对象。

### 协调关系

- **N-3与R-3**共用 `TestConnectionButton` 状态管理，需统一检查listener、timer、取消和过期结果。
- **S-1到S-4**形成搜索展示因果链，S-1实施后必须回查S-2、S-3、S-4。
- **R-2与R-3**都属于异步竞态问题，可共享取消机制设计经验，但没有互相硬阻塞。

### 产品和证据阻塞

- 测试缺口3、4、6、7的推荐产品方案已确认，分别为永久保留批次、时间交错归属、策略C智能排序和批次独立状态；对应测试仍待执行。
- R-1、S-3和阶段0的E2E提交与运行产物已核对，旧版Devlog描述已同步修正；运行产物被 `.gitignore` 排除，后续验收需重新采集。
- 阶段0日志记录的MV3 service worker回收风险（N-V3）已完成代码分析并部分确认；长延迟首响应场景仍需专项运行时验证。
- 阶段0范围外的2个 smoke 业务用例保留为独立边界，不计入阶段0目标 smoke 的通过结论。

## 下一步执行顺序

1. 已完成阶段0、R-1、S-3的提交、Devlog和运行产物对账，并同步正式状态。
2. 阶段2剩余 N-2、S-2、S-4 及阶段3六项问题继续以运行时证据采集和最终复核为主。
3. 在阶段2/3形成完整回归对象后，按依赖顺序执行阶段4的7项测试缺口、8类E2E回归、5项跨域集成和11项运行时假设验证。
4. 每个问题或阶段从 `needs_review` 转为 `completed` 前，必须同步更新 [`修复状态追踪.md`](./修复状态追踪.md)，并满足对应退出条件。

## 统计一致性检查

| 检查项 | 结果 |
| --- | --- |
| 11个问题编号均出现且只归属于一个核心修复阶段 | 通过：R-1、N-2、S-2、S-3、S-4、N-1、N-3、N-4、S-1、R-2、R-3各出现一次 |
| 阶段0至阶段4均有状态记录 | 通过：5个阶段均在阶段进度表出现 |
| 阶段4专项数量为7、8、5、11 | 通过：摘要和专项明细一致 |
| `needs_review`未计入正式完成率 | 通过：核心问题为 `2/11`，阶段为 `2/5` |
| R-1、S-3和阶段0缺口与Devlog一致 | 通过：E2E提交和运行产物已核对，N-V3部分确认风险与范围外 smoke 边界均保留 |
| 当前定位与状态追踪一致 | 通过：阶段0/1为 `completed`，阶段2为 `needs_review`，阶段3为 `in_progress`，阶段4为 `pending`；S-4依赖S-3已解除 |
| 原 `修复状态追踪.md` 已与本文同步 | 通过：本文已同步阶段0、阶段1、R-1、S-3的正式状态和剩余条件 |
