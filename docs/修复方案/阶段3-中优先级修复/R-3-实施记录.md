# R-3 连接测试结果过期覆盖 - 实施记录

- **问题编号**：R-3（MEDIUM）
- **修复阶段**：阶段3 - 中优先级修复
- **Commit**：`2680be2`
- **完成日期**：2026-08-03
- **当前状态**：`needs_review`（代码与单元测试完成，运行时渲染证据待阶段0通道）
- **修复方案**：[`R-3-连接测试结果过期修复方案.md`](./R-3-连接测试结果过期修复方案.md)

## 实际改动

### `Main/src/components/model-settings/TestConnectionButton.tsx`

- 每次测试分配代次，`Promise.race` 返回后先校验代次再写入状态。
- cleanup 会主动 settle 旧请求，避免 listener/timer 清理后 Promise 永久 pending。
- Provider 配置变化通过上层 key 重挂载，组件卸载通过 cleanup 使旧结果失效。

## 验证结果

- `TestConnectionButton.test.tsx`：覆盖正常结果、超时、断线、Provider 变化和卸载，共 18 个测试通过。
- 定向 ESLint：通过。
- `npm run typecheck`：通过（与 N-4 批次合并验证）。
- E2E 旧 Provider 结果和渲染堆栈验证：待阶段0运行时通道。

## 联合审查结论

- N-3 的 `onDisconnect` listener 与 R-3 的代次取消共用请求级资源对象。
- 当前请求的断线结果仍会展示；过期请求的结果会被代次检查丢弃。
- cleanup 多次执行不会重复影响新请求。
