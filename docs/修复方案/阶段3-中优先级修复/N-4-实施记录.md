# N-4 Bilibili 工具网络失败语义 - 实施记录

- **问题编号**：N-4（MEDIUM）
- **修复阶段**：阶段3 - 中优先级修复
- **Commit**：本次 N-4 提交
- **完成日期**：2026-08-03
- **当前状态**：`needs_review`（代码与单元测试完成，运行时错误形态证据待阶段0通道）
- **修复方案**：[`N-4-Bilibili网络失败语义修复方案.md`](./N-4-Bilibili网络失败语义修复方案.md)

## 实际改动

### `Main/src/background/stream.ts`

- 在 `tool-error` 消费边界识别原始 Error、结构化错误对象和字符串化错误。
- `BilibiliNetworkError` 复用 N-1 的 `inferErrorCode`/`friendlyMessage`，保留 cause 链带来的 DNS、HTTP 等细分类别。
- `BilibiliRiskError` 和 `BilibiliApiError` 分别映射为 `BILIBILI_RISK`、`BILIBILI_API`；普通工具错误继续保留工具名上下文。
- 保留 `break streamLoop`，不在缺少运行时证据时改变流终止行为。

## 验证结果

- `stream.test.ts`：43 个测试通过，覆盖结构化网络错误、字符串化 HTTP 错误、风控、业务错误和普通工具错误。
- `npx eslint src/background/stream.ts`：通过。
- `npm run typecheck`：通过。
- `stream.test.ts` 定向 lint 仍有原有 `any`/未使用变量问题，未由本次测试新增。
- E2E `part.error` 实际类型、N-V1 和用户感知证据：待阶段0运行时通道。
